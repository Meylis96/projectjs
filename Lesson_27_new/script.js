5 // Because it return 5 and then after ++ it decreases
(false) NaN - and it is String because empty array takes elems as string
2 because it goes from right to left
12 - and it is String because empty array takes elems as string
1
(true) null - && stamle on falsy and as null is falsy it returned its value
false 1st Boolean 2nd is &&
(false) 3 - because && is in higher priority and it returns 3 // || stumble on true
No. different variables
number because + turns to number
2 because \\ return the first true value and it stops 