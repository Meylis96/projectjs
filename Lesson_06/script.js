let number = 4;

const person = "Alex";

const bool = true; //or false

let und;
console.log(und);

const object = {
    name: 'Meylis',
    age: 24,
    isMarried: true
};

console.log(object["name"]); // it is better to write object.name

let arr = ['plum.jpg', 'orange.png', 6, 'apple.jpeg', {}, []];
console.log(arr[1]);